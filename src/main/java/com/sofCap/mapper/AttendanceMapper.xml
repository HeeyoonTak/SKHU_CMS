<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sofCap.mapper.AttendanceMapper">

	<select id="findAdmin" resultType="String"> <!--학기별 출석체크 회원 리스트 -->
		SELECT DISTINCT u.name
		FROM attendance a JOIN sem_date s ON a.date BETWEEN s.start_date AND s.end_date
		JOIN user u ON a.user_id = u.id
		WHERE s.id = #{s.id}
		AND a.club_id = 1
		ORDER BY u.id
	</select>

	<select id="findDate" resultType="String"> <!--학기별 출석체크 날짜 리스트 -->
		SELECT DISTINCT DATE_FORMAT(a.date,'%Y-%m-%d') date
		FROM attendance a JOIN sem_date s ON a.date BETWEEN s.start_date AND s.end_date
		WHERE s.id = #{s.id}
		AND club_id = 1
		ORDER BY date
	</select>

	<select id="findBySemDate" resultType="AttendanceDto">  <!--학기별 출석체크 리스트 -->
		SELECT a.id
		,a.club_id
		,a.check
		,a.date
		,a.user_id
		,s.sem_name
		,u.name
		FROM attendance a JOIN sem_date s ON a.date BETWEEN s.start_date AND s.end_date
		JOIN user u ON a.user_id = u.id
		WHERE s.id = #{semId}
		AND a.club_id = 1
		ORDER BY a.date, a.user_id
	</select>
	
	<select id="findByDateModal" resultType="String"> <!--모달창에서 날짜 선택시 해당 학기별 회원 리스트-->
		<![CDATA[
		SET @date:=#{date};
		SELECT @semId:=id FROM sem_date WHERE @date >= start_date AND @date < end_date;
		
		SELECT DISTINCT u.name
		FROM attendance a JOIN sem_date s ON a.date BETWEEN s.start_date AND s.end_date
		JOIN user u ON a.user_id = u.id
		WHERE s.id = @semId
		AND a.club_id = 1
		ORDER BY u.id
		]]> <!-- '<'기호 사용하기 위해 선언 -->
	</select>
	
	<select id="findByDate" resultType="AttendanceDto"> <!-- 날짜별 출석 현황 - 수정 페이지 -->
		SELECT a.id
		,a.club_id
		,a.`check`
		,DATE_FORMAT(a.date,'%Y-%m-%d') date
		,u.name
		FROM attendance a JOIN user u ON a.user_id = u.id
		WHERE club_id = 1
        AND a.date = #{date}
		ORDER BY a.user_id
	</select>

	<delete id="delete">
		set sql_safe_updates = 0;
		DELETE a
		FROM attendance AS a
		WHERE a.date = #{date}
		AND a.club_id = 1
	</delete>

	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT attendance(club_id, `check`, date, user_id)
		VALUES (1, #{check}, #{date}, #{user_id})
	</insert>

	<update id="update">
		UPDATE attendance
		SET `check` = #{check}, club_id = 1, date = #{date}, user_id = #{user_id}
        WHERE id = #{id}
	</update>

</mapper>